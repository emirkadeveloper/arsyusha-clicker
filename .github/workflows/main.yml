name: Build Android APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Free Disk Space
        run: |
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /opt/ghc
          sudo rm -rf "/usr/local/share/boost"
          sudo rm -rf "$AGENT_TOOLSDIRECTORY"

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            python3-pip build-essential git python3 python3-dev zip unzip \
            autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev \
            libtinfo5 cmake libffi-dev libssl-dev gettext autopoint libltdl-dev wget

      # --- ИСПРАВЛЕНИЕ: Качаем Ant с флагом -4 (IPv4) и повторами ---
      - name: Pre-install Apache Ant
        run: |
          mkdir -p ~/.buildozer/android/platform/
          # -4 принуждает использовать IPv4 (решает Network unreachable)
          # --tries=5 делает 5 попыток, если сервер тупит
          wget -4 --tries=5 --waitretry=10 https://archive.apache.org/dist/ant/binaries/apache-ant-1.9.4-bin.tar.gz
          tar -xzf apache-ant-1.9.4-bin.tar.gz
          mv apache-ant-1.9.4 ~/.buildozer/android/platform/

      - name: Install Buildozer
        run: |
          pip3 install --upgrade pip
          pip3 install --user --upgrade buildozer
          pip3 install --user "cython<3.0.0"

      - name: Create buildozer.spec
        run: |
          cat <<EOT > buildozer.spec
          [app]
          title = Arsyusha Clicker
          package.name = arsyusha
          package.domain = org.arsyusha
          source.dir = .
          source.include_exts = py,png,jpg,kv,atlas,html,css,js,json,mp3
          version = 1.0
          
          # ПОЛНЫЙ СПИСОК ЗАВИСИМОСТЕЙ ПРОТИВ ВЫЛЕТОВ
          requirements = python3,flask,kivy,pywebview,android,pyjnius,jinja2,werkzeug,markupsafe,itsdangerous,click,sqlite3,openssl,libffi,certifi,chardet,urllib3,requests
          
          # Иконка
          icon.filename = icon.png
          presplash.filename = icon.png
          
          orientation = portrait
          fullscreen = 1
          android.presplash_color = #00C6FF
          
          # API
          android.api = 33
          android.minapi = 21
          
          # Разрешения
          android.permissions = INTERNET,WRITE_EXTERNAL_STORAGE,ACCESS_NETWORK_STATE
          
          # Разрешаем HTTP
          android.manifest.application_attributes = android:usesCleartextTraffic="true"
          
          # AndroidX для WebView
          android.enable_androidx = True
          android.gradle_dependencies = androidx.webkit:webkit:1.4.0
          
          android.archs = arm64-v8a, armeabi-v7a
          android.allow_backup = True
          
          [buildozer]
          log_level = 2
          warn_on_root = 1
          EOT

      - name: Build APK
        run: |
          yes | /home/runner/.local/bin/buildozer android debug

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Arsyusha-APK-Final
          path: bin/*.apk
